<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    use WithoutModelEvents;

    /**
     * Seed the application's database.
     *
     * ────────────────────────────────────────────────────────────────────
     *  Deliberately empty.
     * ────────────────────────────────────────────────────────────────────
     *
     * Every screen in this demo starts with nothing in it, and everything you
     * then see was written BY THE EDITOR. Seeding notes meant the app opened
     * on markup a factory had typed out by hand — which looks like editor
     * output without being any, and quietly answers the one question the demo
     * exists to answer.
     *
     * The factory states are still there for the tests, where hand-written
     * input is the point.
     */
    public function run(): void
    {
        //
    }
}
